// might not use this, just need that bar at the top
export const RecommendationLayout = ({ children }: any) => {
    return (
        <div>
            <div>Layout top...</div>
            {children}
        </div>)
}